#include "main.h"
/**
 *print_sign -  function that prints the sign of a number.
 *@c: print sign of the number
 *
 * Return:
 */
int print_sign(int n)
{
	int n;

	for (n = 0; n > 0; n++)
		printf('+');
		return (1);
		for (n = 0; n == 0; n++)
			printf('+');
		return (0)
			for (n = 0; n <= 0; n--)
				printf('-');
		return (-1);

}


