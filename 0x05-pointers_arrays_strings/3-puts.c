#include "main.h"
#include <stdio.h>
/**
 * _puts - function that prints a string, followed by a new line, to stdout.
 * @str: o stdout.
 *
 * Return: Return a string
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
